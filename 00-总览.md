# MiniMax Music 3 中文知识库

本知识库是「音乐描述词改写器」技能的完整中文资料库：将简短的音乐描述和可选的带标签歌词，转化为详细的 **Music 3.0 结构化描述词（Structured Caption）**（全局元数据、人声细节、编曲），实现精准的生成控制。知识库为纯文本、自包含，无运行时依赖。

## 目录结构

```
MiniMax-Music3知识库/
├── 00-总览.md                    本文件
├── 翻译规范与术语表.md            全库术语基准（新增内容时参考）
└── 音乐描述词改写器/
    ├── 技能说明.md               入口：工作流、输入输出契约、校验规则
    ├── 参考索引/
    │   ├── 流派路由器.md          将流派线索路由到 18 个风格族
    │   └── 索引-*.md             18 个风格族索引，内含紧凑风格卡片
    └── 模版/                     1000 个完整描述词模版
```

## 使用方式

采用渐进式披露（progressive disclosure），智能体只打开真正需要的文件：

1. 阅读 [技能说明.md](音乐描述词改写器/技能说明.md)，了解输入契约与完整工作流。
2. 按 [流派路由器.md](音乐描述词改写器/参考索引/流派路由器.md) 将流派/情绪线索路由到一个主风格族（融合时再加一个次风格族）。
3. 打开对应的风格族索引，比对紧凑卡片，最多挑选三个承担不同角色的参考。
4. 只读取被选中卡片点名的模版，围绕用户需求综合生成一份全新的原创描述词。

## 输入要素

- **Caption（描述词）**（必填）——自然语言的音乐描述，例如：*"一首温暖的原声流行歌曲，亲切的女声、指弹吉他，情绪逐步推进直至宽广的终副歌。"*
- **Lyrics（歌词）**（可选）——包含方括号段落标签的歌词文本，如 `[Verse]`、`[Chorus]`、`[Bridge]`、`[Instrumental]`。歌词只用于推断感情背景，绝不会被复述；只有方括号标签被视为音乐指令。
- **约束**（可选）——期望的时长、输出格式、排除项或创作方向。

## 输出结构

改写后的描述词严格包含三个部分：

- **全局元数据（Global Metadata）**——流派、子流派、速度、情绪走向与制作风格。
- **人声细节（Vocal Details）**——主唱配置、音色、音域、演唱方式、和声/伴唱以及克制的人声效果。
- **编曲（Arrangement）**——按段落展开的时间线，包含乐器生命周期、律动发展与过渡。

改写结果可直接作为 MiniMax Music 3 生成 API 的 `instructions` 字段，原始歌词放在 `input` 字段。
